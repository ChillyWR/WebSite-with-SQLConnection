# WebSite-with-SQLConnection
Simple example of web site for editing predetermined databese.
For the code to work create a database and adjust the project files.

Used python packages:
flask
mysql.connector
